namespace TicketingSystemConsole
{
    // Klasse für Benutzer des Systems
    class User
    {
        // Name des Benutzers
        public string Name { get; set; } = "";

        // Rolle des Benutzers (Admin oder User)
        public string Role { get; set; } = "User";
    }
}
